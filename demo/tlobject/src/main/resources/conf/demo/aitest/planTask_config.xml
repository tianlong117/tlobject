<?xml version="1.0" encoding="UTF-8"?>
<moduleConfig>
    <!--
        TLPlanTaskAgent 配置 —— 任务分解 Agent

        Provider 由 Agent 框架的 defaultLlmProvider 管理（在 <agent> 标签中配置），
        不再需要 resolveLlmProvider() hack。
        Temperature/maxTokens 也在 <agent> 标签中通过 defaultTemperature/defaultMaxTokens 配置。
    -->
    <params>
        <!-- 计划文件输出目录 -->
        <planOutputDir value="data/task_plans/"/>
    </params>
</moduleConfig>
