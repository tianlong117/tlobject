<?xml version="1.0" encoding="UTF-8" ?>
<appConfig>
    <startMsg>
        <!-- 演示各种操作。
           为偷懒，通过设置在action前增加#来不运行该动作, 因为模块没有该动作。
          实际应用中还是通过xml语法注释掉,因为通过命名不存在的动作，虽然没有运行该动作，但是模块要运行该动作的前后其他准备。
        -->
        <msg action="dbToRedis" />
        <msg action="queryRedis"  username="dong1"/>
    </startMsg>
</appConfig>