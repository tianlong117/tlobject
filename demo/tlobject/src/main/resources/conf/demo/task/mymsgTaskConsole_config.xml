<?xml version="1.0" encoding="UTF-8" ?>
<!--
# 列出所有任务
> list

# 启动指定任务
> start task_myModule_syncData

# 停止指定任务
> stop task_myModule_syncData

# 重启所有任务
> restart all

# 关闭任务（彻底停止）
> shutdown task_myModule_syncData

# 关闭线程池
> shutdown pool

# 通过键值对设置任务状态
> t=task_myModule_syncData s=start

# 设置任务状态并修改参数
> t=task_myModule_syncData s=restart delay=30 times=100

# 兼容旧版 run 命令
> run task_myModule_syncData


  -->
<moduleConfig>
    <params>
        <msgTaskModule value="myTaskManger"/>
    </params>

    <cmds>
        <cmd name="p1eat" module="pcboy1" action="eat"  />
        <cmd name="openlog" module="log" action="openLog"  />
      </cmds>
</moduleConfig>